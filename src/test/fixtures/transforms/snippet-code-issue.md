## Snippet followed by inline code

{{premium-note}}

This is text after the snippet. When followed by inline code like `code example`, all text up until the code block gets a box around it.

Here is more text with another inline code example: `another code example`.

## Another section

Normal text without any issues.